server.port=8080

# Mismas variables de entorno (DB_URL, DB_USER, DB_PASSWORD) que ya usa el
# subscriber en docker-compose.yml, asi los dos servicios apuntan a la misma
# base sin duplicar configuracion.
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# La tabla "habitaciones" ya la crea el subscriber (HabitacionDAO) al arrancar,
# asi que el api NO debe tocar el esquema: solo lee/escribe filas.
spring.jpa.hibernate.ddl-auto=none

# Direccionamiento del "sitio": de donde el Controlador lee las lecturas
# (broker MQTT, mismo topico que ya usa subscriber.AppController) y contra
# donde acciona los switches (stub REST). idTermostato e idSwitch en el
# recurso habitacion son solo identificadores; esta es la config concreta.
mqtt.broker.url=${MQTT_BROKER:tcp://mosquitto:1883}
switch.stub.url=${SWITCH_STUB_URL:http://switch-stub:8081}
